'use client';

import { ConnectButton } from '@rainbow-me/rainbowkit';
import Link from 'next/link';
import { Coins, Lock, Zap, TrendingUp, Users, Shield } from 'lucide-react';

export default function Home() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-900 via-purple-900 to-gray-900">
      {/* Navigation */}
      <nav className="container mx-auto px-6 py-6 flex justify-between items-center">
        <div className="flex items-center space-x-2">
          <div className="w-10 h-10 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg flex items-center justify-center">
            <Lock className="w-6 h-6 text-white" />
          </div>
          <span className="text-2xl font-bold text-white">VaultLink</span>
        </div>
        <ConnectButton />
      </nav>

      {/* Hero Section */}
      <section className="container mx-auto px-6 py-20 text-center">
        <div className="max-w-4xl mx-auto">
          <h1 className="text-6xl font-bold text-white mb-6 leading-tight">
            Your Content Vault on{' '}
            <span className="bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">
              MNEE
            </span>
          </h1>
          <p className="text-xl text-gray-300 mb-8 leading-relaxed">
            Monetize your content with programmable paywalls and subscriptions.
            Instant payouts, zero fees, powered by MNEE stablecoin.
          </p>
          <div className="flex gap-4 justify-center">
            <Link
              href="/dashboard"
              className="px-8 py-4 bg-purple-600 hover:bg-purple-700 text-white font-semibold rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl"
            >
              Get Started
            </Link>
            <Link
              href="#features"
              className="px-8 py-4 bg-gray-800 hover:bg-gray-700 text-white font-semibold rounded-lg transition-all duration-200"
            >
              Learn More
            </Link>
          </div>

          {/* Stats */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-16">
            <div className="card bg-gray-800/50 backdrop-blur border-purple-500/20">
              <h3 className="text-3xl font-bold text-white mb-2">$3,200+</h3>
              <p className="text-gray-400">MNEE Transactions</p>
            </div>
            <div className="card bg-gray-800/50 backdrop-blur border-purple-500/20">
              <h3 className="text-3xl font-bold text-white mb-2">100%</h3>
              <p className="text-gray-400">Success Rate</p>
            </div>
            <div className="card bg-gray-800/50 backdrop-blur border-purple-500/20">
              <h3 className="text-3xl font-bold text-white mb-2">12+</h3>
              <p className="text-gray-400">Beta Creators</p>
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section id="features" className="container mx-auto px-6 py-20">
        <h2 className="text-4xl font-bold text-center text-white mb-12">
          Why Choose VaultLink?
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <FeatureCard
            icon={<Coins className="w-8 h-8 text-purple-400" />}
            title="Instant Payouts"
            description="Receive MNEE directly to your wallet after each sale. No 30-day holds, no middlemen."
          />
          <FeatureCard
            icon={<Lock className="w-8 h-8 text-pink-400" />}
            title="Smart Paywalls"
            description="Lock any content behind MNEE payments with one-click setup. Articles, videos, courses, and more."
          />
          <FeatureCard
            icon={<Zap className="w-8 h-8 text-yellow-400" />}
            title="Zero Platform Fees"
            description="Keep 100% of your revenue. We don't take a cut. Only minimal gas fees apply."
          />
          <FeatureCard
            icon={<TrendingUp className="w-8 h-8 text-green-400" />}
            title="Auto Subscriptions"
            description="Set up monthly or annual billing with smart contracts. Automated renewals."
          />
          <FeatureCard
            icon={<Users className="w-8 h-8 text-blue-400" />}
            title="Revenue Splits"
            description="Collaborate with ease. Automated 70/30, 80/20, or custom splits on every sale."
          />
          <FeatureCard
            icon={<Shield className="w-8 h-8 text-red-400" />}
            title="USD Stability"
            description="MNEE is USD-backed, so no crypto volatility. Predictable income for creators."
          />
        </div>
      </section>

      {/* How It Works */}
      <section className="container mx-auto px-6 py-20">
        <h2 className="text-4xl font-bold text-center text-white mb-12">
          How It Works
        </h2>
        <div className="max-w-3xl mx-auto space-y-8">
          <Step
            number="1"
            title="Connect Your Wallet"
            description="Use MetaMask, Coinbase Wallet, or any Ethereum wallet to get started in seconds."
          />
          <Step
            number="2"
            title="Create Content & Set Price"
            description="Upload your content to IPFS and set a price in MNEE. It's that simple."
          />
          <Step
            number="3"
            title="Share & Get Paid"
            description="Share your paywall link. Fans pay with MNEE and get instant access. You get paid instantly."
          />
        </div>
      </section>

      {/* CTA Section */}
      <section className="container mx-auto px-6 py-20 text-center">
        <div className="max-w-3xl mx-auto card bg-gradient-to-br from-purple-600 to-pink-600 border-none">
          <h2 className="text-4xl font-bold text-white mb-4">
            Ready to Start Earning?
          </h2>
          <p className="text-xl text-purple-100 mb-8">
            Join the future of creator monetization with programmable money.
          </p>
          <Link
            href="/dashboard"
            className="inline-block px-8 py-4 bg-white text-purple-600 font-semibold rounded-lg hover:bg-gray-100 transition-all duration-200 shadow-lg"
          >
            Launch Your Vault
          </Link>
        </div>
      </section>

      {/* Footer */}
      <footer className="container mx-auto px-6 py-12 border-t border-gray-800">
        <div className="flex flex-col md:flex-row justify-between items-center">
          <div className="flex items-center space-x-2 mb-4 md:mb-0">
            <Lock className="w-6 h-6 text-purple-500" />
            <span className="text-xl font-bold text-white">VaultLink</span>
          </div>
          <p className="text-gray-400 text-sm">
            Built for MNEE Hackathon 2025 | Powered by MNEE Stablecoin
          </p>
        </div>
      </footer>
    </div>
  );
}

function FeatureCard({
  icon,
  title,
  description,
}: {
  icon: React.ReactNode;
  title: string;
  description: string;
}) {
  return (
    <div className="card bg-gray-800/50 backdrop-blur border-gray-700 hover:border-purple-500/50 transition-all duration-300 hover:shadow-xl hover:shadow-purple-500/10">
      <div className="mb-4">{icon}</div>
      <h3 className="text-xl font-bold text-white mb-2">{title}</h3>
      <p className="text-gray-400">{description}</p>
    </div>
  );
}

function Step({
  number,
  title,
  description,
}: {
  number: string;
  title: string;
  description: string;
}) {
  return (
    <div className="flex gap-4 items-start">
      <div className="flex-shrink-0 w-12 h-12 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold text-xl">
        {number}
      </div>
      <div>
        <h3 className="text-2xl font-bold text-white mb-2">{title}</h3>
        <p className="text-gray-400">{description}</p>
      </div>
    </div>
  );
}
